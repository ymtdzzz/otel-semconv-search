import { describe, expect, it } from "vitest";

// Placeholder test to confirm the Vitest wiring works. Real build-data unit
// tests arrive in slice 4.
describe("scaffold", () => {
  it("runs vitest", () => {
    expect(1 + 1).toBe(2);
  });
});
